fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0

# Google Gemini
google-generativeai>=0.7.0

# Groq API
groq>=1.0.0

# HTTP client
httpx>=0.27.0

# DOCX export
python-docx>=1.1.0

# ML stack — already installed on Mac Mini, listed for completeness
# torch, transformers, sentence-transformers, pinecone-client
# are imported from legal-data scripts via sys.path, not reinstalled here
